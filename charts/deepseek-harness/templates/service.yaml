apiVersion: v1
kind: Service
metadata:
  name: {{ include "deepseek-harness.fullname" . }}
  labels:
    {{- include "deepseek-harness.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "deepseek-harness.selectorLabels" . | nindent 4 }}

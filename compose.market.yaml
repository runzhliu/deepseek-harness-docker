services:
  deepseek-harness:
    build:
      dockerfile: Dockerfile.market
      args:
        BASE_IMAGE: ${DSH_IMAGE_REPOSITORY:-runzhliu/deepseek-harness}:${DSH_IMAGE_VERSION:-0.1.2-rc.1-r1}
        DSH_MARKET_VERSION: ${DSH_MARKET_VERSION:-1.38.1}
        MARKET_IMAGE_VERSION: ${MARKET_IMAGE_VERSION:-0.1.2-rc.1-r1-market.1}
        NODE_IMAGE: ${NODE_IMAGE:-node:24-trixie}
    image: ${DSH_IMAGE_REPOSITORY:-runzhliu/deepseek-harness}:${MARKET_IMAGE_VERSION:-0.1.2-rc.1-r1-market.1}
